@model IEnumerable<Example.StudentsManagement.Models.Administrator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @if (User.HasPermissionIn(new string[] { AppPermissions.MANAGE_ADMINISTRATOR_PROFILE }))
    {
        <span>
            @Html.ActionLink("Create New", "Create")
        </span>
    }
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @if (User.HasPermissionIn(new string[] { AppPermissions.MANAGE_ADMINISTRATOR_PROFILE }))
                {
                    <span>
                        @Html.ActionLink("Edit", "Edit", new { administratorId = item.Id }) |
                        @Html.ActionLink("Delete", "Delete", new { administratorId = item.Id })|
                    </span>
                }
                @if (User.HasPermissionIn(new string[] { AppPermissions.MANAGE_ADMINISTRATOR_PROFILE, AppPermissions.VIEW_ADMINISTRATOR_PROFILES }))
                {
                    <span>
                        @Html.ActionLink("Details", "Details", new { administratorId = item.Id })
                    </span>
                }

            </td>
        </tr>
    }

</table>
